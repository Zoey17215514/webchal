Run locally (Python):

python3 -m venv venv && source venv/bin/activate

pip install -r requirements.txt

Ensure flag.txt is in the same folder.

python app.py

Visit http://127.0.0.1:5000/search and play.